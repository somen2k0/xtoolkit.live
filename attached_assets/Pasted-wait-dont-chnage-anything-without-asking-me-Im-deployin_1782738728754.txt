wait dont chnage anything without asking me

Im deploying my website with vercel so i dont need any build error take care of this and

I need these fixes 

Footer logo — Replaced the "XT" text placeholder with your actual logo.png image, matching the navbar exactly.

Color fixes across all pages:

Home page — Hero gradient changed from #474bff (blue) → Replit primary orange-red. FAQ accordion changed from hardcoded #e5e7eb, bg-white, #374151, #6b7280 colors → full theme variables (border-border, bg-card, text-foreground, text-muted-foreground, text-primary).

Navbar badges — "Popular" badge: amber → primary orange-red. "New" badge: emerald → theme success. "Soon" badge: slate → muted. Operational dot: emerald → success.

Footer — Operational status dot: emerald-500 → success. Subscribed confirmation: emerald → success.

Character Counter — Platform limit bar colors (blue/purple/sky/pink) → primary theme variants. Progress bar colors: bg-yellow-500/bg-green-500 → bg-warning/bg-success. Status text colors updated the same way.

AI Detector — All purple buttons, focus rings, style selectors, and humanized result panel → primary orange-red. Wand icons updated too.

Base64 Encoder — Size increase text: yellow → warning. Decode box: green → success. All code language labels (bash/openssl/python/node/powershell) → theme-consistent colors using primary, accent, muted, success, and secondary.

The fix is applied to MiniToolLayout.tsx — since every tool page shares this component, the FAQ section is now fixed across all 44 tool pages at once. Both the background color (bg-[#f9fafb] → bg-background) and all the FAQ accordion colors (white/blue → theme variables) are updated.

 🔲 Tool card empty space (highest impact)
On tool pages like JSON Formatter, there's a large blank gap in the tool card before the content appears. The ad slot at the top is rendering as invisible space. Either show a skeleton placeholder or remove the top ad slot from tool pages — it's hurting first impressions.

 📱 Cookie banner overlaps tool content on mobile
The banner sits in the middle of the viewport on small screens and hides active content. Pinning it to the bottom of the screen with a smaller height would solve this.


several issues. Let me list what's wrong:

theme-color is purple #6366f1 — should be your Replit orange #F5390A
Anti-FOUC background is white #ffffff — causes a white flash before React loads instead of your beige theme
Tool count mismatch — index.html says "44+" everywhere but your app shows 44 tools
SeoHead doesn't update og:image — every page shares the homepage OG image with no per-page override
SeoHead missing twitter:card update — card type never changes on navigation