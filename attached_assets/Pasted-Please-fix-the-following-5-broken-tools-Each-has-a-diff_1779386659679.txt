Please fix the following 5 broken tools. Each has a different issue.
Do not touch anything else.

---

### FIX 1 — AI Bio Generator + AI Text Detector (Groq API)

The Groq API calls are failing because the server is returning a plain
text error instead of JSON. This means GROQ_API_KEY is either missing
from the Vercel environment or the error is not being caught properly.

Do this:
1. In all Groq API routes (/api/generate-bio, /api/ai-detector/detect,
   /api/ai-detector/humanize), wrap the entire handler in a try/catch
2. If GROQ_API_KEY is missing or undefined at runtime, return a proper
   JSON error response immediately:
   res.status(500).json({ error: "AI service is temporarily unavailable." })
   Never let the server return plain text or HTML to the frontend.
3. Add a startup check that logs whether GROQ_API_KEY is defined:
   console.log('GROQ_API_KEY present:', !!process.env.GROQ_API_KEY)
4. In the frontend components for AI Bio Generator and AI Text Detector,
   improve the error handling to show a user-friendly message:
   "AI service is temporarily unavailable. Please try again later."
   instead of a raw JSON parse error.

IMPORTANT: After this fix, I also need to manually add GROQ_API_KEY
to Vercel environment variables. Add a comment in the code noting:
// Requires GROQ_API_KEY in Vercel environment variables

---

### FIX 2 — Image Compressor

The Canvas API compression is failing with "Failed to compress one or
more images." Fix the compression logic:

1. Rewrite the image compression function to use this reliable approach:
   - Create an HTMLImageElement and set its src to a FileReader result
   - Wait for the image onload event before drawing to canvas
   - Use canvas.toBlob() with the correct MIME type and quality parameter
   - For PNG files: convert to JPEG for compression (PNG canvas export
     doesn't support quality parameter), unless user wants to keep PNG
   - For JPEG/WebP: use canvas.toBlob(callback, 'image/jpeg', quality/100)
   - Wrap everything in a Promise so it works with async/await
   - Add proper error handling per file

2. The quality slider value (10-100) should be divided by 100 when
   passed to toBlob (e.g. 86% → 0.86)

3. If a file fails to compress, show which specific file failed
   instead of a generic error, and still process the other files

4. Test with JPEG, PNG, and WebP input types

---

### FIX 3 — OG / Twitter Card Preview

The /api/og-preview endpoint cannot fetch external URLs when deployed
on Vercel because of network restrictions.

Fix this:
1. Add these headers to the outgoing fetch request in /api/og-preview:
   'User-Agent': 'Mozilla/5.0 (compatible; Googlebot/2.1)'
   'Accept': 'text/html,application/xhtml+xml'
   'Accept-Language': 'en-US,en;q=0.9'

2. Add a timeout of 10000ms using AbortController

3. Add a CORS-friendly fallback: if direct fetch fails, try fetching
   through https://api.allorigins.win/get?url=[encoded_url]
   and parse the contents field from the response

4. Return proper JSON error if both attempts fail:
   { error: "Could not fetch page. The site may block external requests." }

5. In the frontend, show this specific error message to the user
   instead of the generic "Network error"

---

### FIX 4 — Contact Form

The contact form is failing with "Something went wrong."
The Web3Forms integration is broken.

Fix this:
1. The contact form should submit DIRECTLY from the frontend to
   Web3Forms API (https://api.web3forms.com/submit) using the
   public access key — do NOT route through the backend for this.

2. Replace the current /api/contact backend call with a direct
   frontend fetch to Web3Forms:

   const response = await fetch('https://api.web3forms.com/submit', {
     method: 'POST',
     headers: { 'Content-Type': 'application/json' },
     body: JSON.stringify({
       access_key: 'YOUR_WEB3FORMS_PUBLIC_KEY',
       name: formData.name,
       email: formData.email,
       subject: formData.subject,
       message: formData.message
     })
   });

3. Get the Web3Forms public key from the existing backend
   GET /api/contact/token endpoint and use it in the frontend,
   OR hardcode the public key directly in the frontend component
   (Web3Forms public keys are safe to expose in frontend code).

4. On success (response.ok === true), show:
   "Message sent! We'll get back to you soon." (green toast)
   On failure, show:
   "Could not send message. Please email us directly at support@xtoolkit.live"
   (red toast)

---

After all 4 fixes are done:
- Rebuild the project and confirm no TypeScript errors
- List every file that was changed
- Add a comment // FIXED: [issue name] near each fix so I can find them