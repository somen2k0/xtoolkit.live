Please implement a complete infrastructure overhaul across our codebase to fix an issue where the Google AdSense crawler flags our site as "Thin Content" due to how our React SPA handles routing and rendering. Follow these absolute technical steps carefully:

1. Update `src/main.tsx`:
   - Change the React entry point from `createRoot(document.getElementById("root")!).render(<App />)` to use hydration. 
   - Import `hydrateRoot` from "react-dom/client" and execute `hydrateRoot(document.getElementById("root")!, <App />)`. This stops React from purging our crawlable HTML data during boot.

2. Update `artifacts/x-checker/prerender.mjs`:
   - Modify the string template injected into `<div id="root">` at build time so that it mimics a structural layout. 
   - Ensure it generates a structure containing:
     - An <h1> header title and a paragraph description matching the targeted tool.
     - A `<main class="mb-12 p-6 border rounded-xl bg-card">` layout block enclosing a `<noscript>` fall-back warning.
     - An `<article class="prose dark:prose-invert max-w-none border-t pt-8 mt-8">` node which dynamically drops in the long-form educational, keyword-rich article string corresponding to that tool route.
   - This prevents structural mismatches during hydration.

3. Implement the Layout Wrapper:
   - Create or adjust a main layout wrapper component (e.g., `ToolLayout.tsx`) so that the interactive user forms and the long-form descriptive article text sit in the exact same matching HTML layout blocks (`<main>` and `<article>`) generated by our build-time pre-renderer script.

4. Adjust Footer Links for Policy Navigation:
   - Look at our Footer component. Change all legal compliance links (About, Privacy Policy, and Terms of Service) from our client-side router (`Wouter`) links over to standard HTML `<a>` tags with `rel="external"` attributes. This forces hard browser reloads so the AdSense tester bot can browse them natively via standard network requests.

5. Optimize Tailwind v4 Styles:
   - Check `vite.config.ts`. Ensure our CSS bundle optimization pipeline is utilizing Vite's native CSS minification capabilities (such as Lightning CSS options) to compress our compiled stylesheet bundle below its current heavy size, completely removing any render-blocking timeout windows during simulated mobile tests.

Once you have systematically modified these components, verify that the application compiles perfectly with zero build errors or typescript compilation mismatch warnings.
